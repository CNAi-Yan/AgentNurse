spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agent_nurse_dev?useSSL=false&serverTimezone=UTC&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    com.agentnurse: DEBUG
